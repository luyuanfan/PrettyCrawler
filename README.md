# PrettyCrawler

clone this repo.

make a virtual env.
```bash
$ python3 -m venv .venv
$ . .venv/bin/activate
```

install django.
```bash
pip install Django
```

to run server, go to the crawler directory and run:

```bash
$ python manage.py runserver
```